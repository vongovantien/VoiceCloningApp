# Flask Web Framework
Flask>=2.3.0
Werkzeug>=2.3.0

# Authentication
bcrypt>=4.0.0
PyJWT>=2.8.0

# Database
# sqlite3 is built-in

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0
numpy>=1.24.0

# TTS - F5-TTS dependencies
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.0
einops>=0.6.0
vocos>=0.0.3
bitsandbytes>=0.40.0
jieba>=0.42.0
pypinyin>=0.49.0
cached_path>=1.4.0
datasets>=2.14.0

# Speech Recognition (for quality metrics)
faster-whisper>=0.10.0

# Model/Config
hydra-core>=1.3.0
omegaconf>=2.3.0

# HTTP Requests
requests>=2.31.0

# Utilities
click>=8.1.0
tqdm>=4.65.0
psutil>=5.9.0
pyyaml>=6.0

# Image Processing (for avatar/cover uploads)
Pillow>=10.0.0

# Gradio (optional - for training UI)
gradio>=4.0.0
